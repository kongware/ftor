const akeys = require("../reflection/keys");
const comp = require("../../../../polymorphic/composition/comp");
const values = require("../..object/array/iteration/values");

module.exports = keys = kv => comp(values) (akeys) (kv);